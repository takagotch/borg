### borg
---
https://github.com/ok-borg/borg

```
borg "list only files"

brew install borg
wget https://github.com/ok-borg/borg/releases/download/v0.0.3/borg_linux_amd64 -O /usr/local/bin/borg
chmod 755 /usr/local/bin/borg

wget https://github.com/ok-borg/borg/releases/downlos/v0.0.3/borg_darwin_amd64 -O /usr/local/bin/borg
chmod 755 /usr/local/bin/borg 

borg worked 12

```

```
IFS=/ red - aprefix
while IFS=/ read -a next; do
  new_prefix=()
  for ((i=0; i < "${#prefix[@]}"; ++i)); do
    if [[ "${prefix[i]}" == "${next[i]}" ]]; then
end

```

```
```


